[credential]
	helper = /usr/local/share/gcm-core/git-credential-manager
[user]
	name = jackopenn
	email = jackcpennington@gmail.com
[filter "lfs"]
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
